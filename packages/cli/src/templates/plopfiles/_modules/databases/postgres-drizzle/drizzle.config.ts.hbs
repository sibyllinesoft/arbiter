import type { Config } from "drizzle-kit";

export default {
  schema: "./{{backendDir}}/src/db/schema.ts",
  out: "./{{backendDir}}/drizzle",
  driver: "pg",
  dbCredentials: {
    connectionString: process.env.DATABASE_URL!,
  },
} satisfies Config;
