apiVersion: v1
kind: Service
metadata:
  name: {{kebabCase name}}
  labels:
    app: {{kebabCase name}}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{kebabCase name}}
